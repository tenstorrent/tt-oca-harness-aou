# SPDX-License-Identifier: Apache-2.0
# Copyright (c) 2026 Tenstorrent USA Inc
#
# Single source of truth for CI tool binary pins.
# Kept outside .github/workflows/ so GITHUB_TOKEN can bump these without
# the PAT `workflow` scope (which is not available as a workflow
# `permissions:` key).
#
# Consumed by lint.yml / cocotb.yml and scripts/check_tool_versions.sh.

VERILATOR_VERSION=5.048
VERIBLE_VERSION=v0.0-4157-gfdbac312